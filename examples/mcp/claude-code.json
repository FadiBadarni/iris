{
  "_comment": "Drop into ~/.claude/mcp.json (global) or .claude/mcp.json (project). Requires `iris` installed in the workspace so npx can resolve iris-mcp from node_modules.",
  "mcpServers": {
    "iris": {
      "command": "npx",
      "args": ["-y", "iris-mcp"]
    }
  }
}
